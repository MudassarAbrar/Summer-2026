# CLAUDE.md
This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Common Commands
- **Build**: `npm run build` (or appropriate build command from package.json, if available)
- **Lint**: `npm run lint`
- **Start Local Server**: `npm start` (or `npm run dev` for development server)
- **Run Tests**: `jest` (or `npm test` for Jest, `yarn test` for Yarn, or [specific framework] tests)
- **Run Single Test**: `jest <test-file>` (adjust syntax based on test runner)

## High-Level Architecture

1. **Core Components**
   - [key_component]: [Brief description of main feature/module]
   - [another_key_component]: [Description of interconnected system]

2. **Data Flow**
   - [source] → [processing] → [storage]

3. **Dependencies**
   - [External libraries]
   - [Internal modules working together]

4. **Key Development Files**
   - [critical-file1.ts]: [Purpose in architecture]
   - [critical-directory]: [Hierarchical role]

## Customization Rules
[A condensation of cursor/copilot rules or important development constraints from existing configs, e.g.: "Use snake_case for API endpoints"]

[Additional repository-specific instructions derived from code patterns or project goals, avoiding generic advice]
# Recall — Mobile App (placeholder)

This folder will contain the Expo React Native app for Recall.

Planned structure:
- `app/` (Expo Router pages)
- `components/`
- `hooks/`
- `stores/`
- `lib/`

To scaffold the real app later run `npx create-expo-app .` or `npx expo init`.
